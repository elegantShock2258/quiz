import { Notifier } from "./notifier";
import { SnackbarNotification, ToasterNotification } from "./types";
declare const snackbar: (settings: Partial<SnackbarNotification>) => void;
declare const toast: (settings: Partial<ToasterNotification>) => void;
declare const append: (el: HTMLElement) => void;
export { Notifier, snackbar, toast, append };
