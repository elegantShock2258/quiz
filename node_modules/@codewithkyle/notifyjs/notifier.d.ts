import { SnackbarNotification, ToasterNotification } from "./types";
export declare class Notifier {
    private snackbarQueue;
    private toaster;
    private time;
    constructor();
    private uid;
    private loop;
    snackbar(settings: Partial<SnackbarNotification>): void;
    toast(settings: Partial<ToasterNotification>): void;
    private getShell;
    append(el: HTMLElement): void;
}
